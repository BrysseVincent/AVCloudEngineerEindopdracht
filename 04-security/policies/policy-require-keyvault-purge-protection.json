{
  "$schema": "https://schema.management.azure.com/schemas/2019-09-01/policyDefinition.json#",
  "name": "require-keyvault-purge-protection",
  "displayName": "Key Vault should have purge protection enabled",
  "description": "Denies creation or update of Key Vaults without purge protection enabled. Purge protection prevents permanent deletion of Key Vault and its objects during the retention period. Required for NIS2 compliance and protection against accidental or malicious data deletion.",
  "mode": "Indexed",
  "metadata": {
    "category": "Key Vault",
    "version": "1.0.0",
    "author": "Vincent Brysse"
  },
  "parameters": {
    "effect": {
      "type": "String",
      "allowedValues": ["Deny", "Audit", "Disabled"],
      "defaultValue": "Deny"
    }
  },
  "policyRule": {
    "if": {
      "allOf": [
        {
          "field": "type",
          "equals": "Microsoft.KeyVault/vaults"
        },
        {
          "field": "Microsoft.KeyVault/vaults/enablePurgeProtection",
          "notEquals": true
        }
      ]
    },
    "then": {
      "effect": "[parameters('effect')]"
    }
  }
}
